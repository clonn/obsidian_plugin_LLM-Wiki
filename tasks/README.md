# Tasks

One `.md` per task. Each task file starts with a checkbox on line 1:

```markdown
- [ ] Task title
```

Flip to `- [x]` and commit when done. Tasks are numbered to make ordering
explicit. Sub-tasks live inside the task file.
