- [ ] Final verification pass

## Done when
- `kb-verifier` subagent run on the organized vault returns PASS
- Plugin builds clean (`npm run build`)
- `uv run tools/lint/lint.py` returns 0
- Every task file in `tasks/` is checked off
- Final commit lands with message summarizing the outcome
- User has a one-paragraph "how to use this" written to `README.md` at repo
  root
