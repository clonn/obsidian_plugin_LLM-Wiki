# Node / Obsidian plugin
node_modules/
plugin/main.js.map
plugin/dist/
plugin/.obsidian-build-cache/
# NOTE: plugin/main.js is checked in so the vault symlink picks up a working
# build without needing `npm run build` on every clone.

# Python / uv
.venv/
__pycache__/
*.pyc
.ruff_cache/
.pytest_cache/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Local env / secrets
.env
.env.*
!.env.example

# Tool output (audit reports are committed; caches are not)
tools/**/__cache__/
